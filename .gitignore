# 忽略 Rust 编译器生成的构建输出目录
/target/

# 忽略 Cargo 的锁定文件（如果是二进制应用，建议提交 Cargo.lock；如果是库，则可选）
# 如果你是开发库(Library)，通常不需要忽略；如果是应用(Binary)，建议保留。
# Cargo.lock

# 忽略 IDE 和编辑器生成的临时文件
.idea/
.vscode/
*.rs.user

# 忽略二进制文件和临时文件
*.exe
*.dll
*.so
*.dylib
*.o
*.out

# 忽略操作系统自动生成的文件
.DS_Store
Thumbs.db

# 忽略所有编辑器的临时备份文件
*~